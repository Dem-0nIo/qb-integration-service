target/
*.class
.idea/
*.iml
.DS_Store
*.log
HELP.md
*.sql
*.dump
*.csv
/data/
/exports/

# Variables de entorno — nunca subir al repo
.env
.env.production
.env.local

# El .env.development ya existe con ese nombre — agregarlo explícitamente
.env.developmenttokens.json
**/loadtest/tokens.json

# Endpoint temporal de dev (no debe ir a producción)
**/controller/DevTokenController.java

# Tokens generados localmente (regenerables, no versionar)
tokens.json
**/loadtest/tokens.json
loadtest/clientes-reales.txt
